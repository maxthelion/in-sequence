On the tracks page, make kit tracks show in the grid like normal tracks. Take out most of the UI stuff surrounding it. Show the contained tracks as cells within the cell, for indication purposes. Expand button should be in the cell. When clicked, have the kit as a cell, and the parts as cells following it. No wrapper around them.

Status: RESOLVED (uncommitted, branch audio-routing-cleanup) — Sources/UI/TracksMatrixView.swift. Removed the bordered green GroupSectionView wrapper (kit name title, "N tracks" badge, "LINKED" pill, "Shared destination not assigned" subtext, section fill/outline). matrixSections now renders ONE flat LazyVGrid: ungrouped track cells, then per kit a single KitMatrixCard (replaces the old CollapsedKitCell + GroupSectionView, reuses TrackMatrixCard's minHeight-132 footprint + outline grammar) followed inline by its part cells (normal TrackMatrixCard) when expanded, then the add-track tile. The expand/collapse affordance now lives ON the kit cell as a circular icon button (top-right, where a normal cell's mute toggle sits); collapsed shows a small nested thumbnail strip of the contained part names inside the kit cell for indication, hidden when expanded. Selection/open still speak for the whole kit; accessibility IDs kit-collapsed-cell / kit-expand / kit-collapse / kit-card-select-mark preserved. Build SUCCEEDED; realtime-path-lint + runtime-ownership-lint exit 0; TracksPageInvalidationTests (5) pass. UI-only — no audio/routing/engine changes. Visual "looks right" is the owner's call.
