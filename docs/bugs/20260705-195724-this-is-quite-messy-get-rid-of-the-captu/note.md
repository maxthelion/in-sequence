This is quite messy. get rid of the capture history title and line. The capture for each part doesn't have to represent pitches. It's just the triggered steps. So it can be vertically compressed. There should be a border around the whole bottom area. Save capture should be in the menu above the ui, not below. When selected, the pattern selection ui should be the pattern numbers at the top. Potentially pulsing until a selection is made

Screenshots:
- 29f-drum-kit-capture-save-slot.png

Status: RESOLVED pending commit - kit capture now uses compact triggered-step
rows, a bordered bottom history area, top-row pattern numbers as the save
target, and deterministic QA capture history published through bug-reporter.
