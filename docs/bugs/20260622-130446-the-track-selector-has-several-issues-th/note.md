The track selector has several issues. The whole cell should be clickable and coloured based on whether it's in the selection. The "mono" text is useless. The button for selecting tracks should be in the other menu bar on the left. It should be a single button with saying how many tracks are selected. Track selector button is redundant, it gets rolled in. When viewing the selection of tracks, "all" and "clear" should be options. Clicking the main button should close the view down.

---
RESOLVED 2026-06-22: global-apply track selector reworked — single count button ("N selected"/"ALL N TRACKS") in the left menu row toggles the selector; track cells are whole-cell tappable tiles, name-only (removed "MONO" subtitle + radio circle); selected cells fill amber; "All"/"Clear" actions replace Hide/All Tracks.
VERIFIED — independent agent PASS on core C1 (single count button), C2 (no MONO/radio), C3 (All+Clear), C4 (whole-cell tiles); evidence: 13-phrase-global-apply.png (closed) + 13a-phrase-global-apply-track-selector.png (open). C5 amber-on-select not fixture-exercised (empty scope = all tracks); colour logic is isSelected?amber:neutral. FOLLOW-UP: add a global-apply preselect capture to show a selected (amber) cell.
