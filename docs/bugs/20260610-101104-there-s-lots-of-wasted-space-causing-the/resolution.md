# Resolution — branch fix/ui-consistency-bugs

- "Pattern editor" subtitle and the long footer explainer sentence removed
  (footer keeps a compact note count).
- Lane and Length controls stay in the clip title row.
- The two-row sequencer layer grid is now a single line with up/down arrows
  that cycle Steps / Velocity / Chance / assigned macro lanes, plus a small
  "Assign Macro" affordance when an empty macro slot exists.
- Fill preview moved out of the tab row to the page header (see 094610).
