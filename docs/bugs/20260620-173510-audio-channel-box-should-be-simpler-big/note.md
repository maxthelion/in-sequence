Audio channel box should be simpler. big colourful element represeenting the audio input with the levels as part of it. Plus button if nothing selected. Some sort of interface that can be clicked to allow changing the interface (perhaps as modal).

Screenshots:
- 24-audio-idle.png

RESOLVED 2026-06-20: Redesigned the Audio Channel card in AudioInputRuntimePanel.inputCard (Sources/UI/Track/TrackWorkspaceView.swift). Removed the eyebrow + disabled Configure button + inline Menu picker + separate level-meter panel. Now: when no input device is available, the card shows a StudioAddCard "+" empty state ("Add Audio Input"); when a channel is selectable it shows ONE big colourful (success-accent) tappable element with the selected channel name, channel-requirement label, a "Change" affordance, and the live AudioInputLevelMeters composited into the same accent surface (text/meters drawn in StudioTheme.background so they stay legible on the fill). Tapping either opens a new AudioInputChannelPickerSheet (StudioModal) listing the already-available mono/stereo channel options (reusing AudioInputChannel.mono/stereoOptions); selecting one calls session.setAudioInputChannel, the same underlying selection the old picker used. No device-enumeration / new-input creation added (that remains the separate pending feature). Bottom three cards stay bottom-aligned. Verified via qa-surface-coverage (24-audio-idle = "+" empty state, 25-audio-live = big green element with integrated meters).
