the element at the bottom should be in the same horizontal bar as the selection button.  1 selected is redundant. make it buttons and take out all the extra text

Screenshots:
- 02a-tracks-selection-actions.png

RESOLVED 2026-06-20: Merged the selection actions into the Select bar as compact inline buttons (Layer perform / Same value / disabled Create performance group); removed the separate actions nav, the "N selected" text and per-action descriptions.
