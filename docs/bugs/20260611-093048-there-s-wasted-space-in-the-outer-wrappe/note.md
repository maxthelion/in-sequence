There's wasted space in the outer wrapper that contains "Scenes" and "perform".  The perform button can be inside the nested container. There's a more general point that we need to review the nesting of ui containers. Often the outer one is not needed and introduces extra padding and makes alignment ugly. Review other pages like phrases for this pattern.

Screenshots:
- 06-scenes-perform.png
