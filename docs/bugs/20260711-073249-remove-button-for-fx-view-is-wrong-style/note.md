remove button for fx view is wrong style. removing effect killed audio

Status: RESOLVED (UI style scope)

The shared FX editor now uses the canonical circled xmark icon control for
remove. The reported audio-loss behavior is intentionally outside this UI-only
fix and requires separate audio/session investigation.
