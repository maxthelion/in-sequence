several buttons are quite fussy about where they are clicked. The whole thing should be its hitarea

Status: RESOLVED (uncommitted) — Root cause: pill/chip buttons whose label has a `Color.clear` fill when unselected (and no `.contentShape`) had a hit area limited to the icon/text glyph, so taps on the padding/capsule were dead. Fixed by adding `.contentShape(...)` matching each control's outline shape, at the shared/component level: StudioTopBar nav pills (Sources/UI/StudioTopBar.swift), StudioSegmentedControl chip (Sources/UI/Theme/StudioSegmentedControls.swift), TransportModePicker + TransportButtonStyle (Sources/UI/TransportBar.swift), StudioFXOptionRow (Sources/UI/Theme/StudioCards.swift), and the DrumKitMatrixView fill-mode / layer / bar-page chips (DrumKitMatrixView+Matrix.swift, +Header.swift). Appearance/layout unchanged (contentShape only affects hit-testing). Build SUCCEEDED; both diagnostics lints exit 0. Click-confirmation (tapping the padding now registers) is the owner's to verify at runtime.
