The fx buttons of a bus are fussy about where they are clicked. The whole thing should work

Status: RESOLVED

The shared compact FX insert button now claims the full rendered tile width and
applies the row content shape at both the label and button levels. Bus and
master insert rows use this shared interaction grammar, so the whole tile opens
the FX editor instead of only the insert name.
