Include the pitch generator in the captures. Re-organize this view of pitch generation. Take out the useless information in the header (generator 2 etc). Move bake into the top line with the cross. Have the piano keys run across the top of the view as the first item. Then make root note into a rotary. spread into a rotary. I don't know what selection means, but that should probably be a rotary. Scale can be a dropdown, but we need some sort of standard element for that which fits into the overall aesthetic.

Status: RESOLVED 3f4ad358
