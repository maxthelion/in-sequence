There are too many levels of navigation. I think the layer selector widget should be far less wide. That bar and the orange outline one with performance could be merged if all the copy gets deleted from the latter

Screenshots:
- 08-phrase-layers-pattern.png
