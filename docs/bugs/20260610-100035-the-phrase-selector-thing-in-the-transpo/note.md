The phrase selector thing in the transport is awkward. It should have a matrix style button to open a modal with all the phrases in a 8x8 grid. This should highlight the one that is playing with a progress bar indicating when it will finish. By default, clicking one of the phrases should cue it to be launched when the current one finishes. Shift clicking switches to it immediately.
