The top section of slicer, normal track, kit and audio are different. Let's standardize on the compressed version in the drum kit version

Status: RESOLVED pending commit - single-track headers now share the drum-kit rounded header shell with pattern slots inside the header; duplicate standalone pattern panels were removed from normal, slicer, and audio-input track bodies while preserving the clip-history save-slot picker.
