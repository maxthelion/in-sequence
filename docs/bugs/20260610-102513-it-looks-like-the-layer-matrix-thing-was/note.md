it looks like the layer matrix thing was built, but in quite a weird way. It's on the phrase view, but the track view is still the old model. I wanted the individual note repeat options to be their own cells, not tiny ones. The idea with those ones is that they act like a toggle from a ui point of view, but off is normal, and on is the setting that's been chosen. Also, there's no reason to have patterns like that. it can just be a layer that gets selected. finally, it's not respecting the 8x8 grid idea. It's meant to be similar to the interface it's going to be working with. but it's only 4 cells wide in its matrix. Also they don't need a button saying select. The whole thing should be a button that selects. Also, latch is not a layer, it's a description of ui behaviour
