The mixer layout is a big mess. There are multiple controls for adding busses. Busses are rendered lower than other channel strips. The volume bars for the normal channels and busses don't show levels like the master. Pan should be below the volume control with a side to side ui element. The title of each channel is different sized. They should be small. Send a and b have too much text in them. Their volume bar should be aligned with the other channels. Sends should be grouped next to the master, rather than the other channels. There should be a consistent space allocation for fx in channels, busses, sends and masters so that everything lines up. Send fx rotaries for a channel should move in place, rather than opening a ui element with a slider. They also seem to have some debouncing issues or something. clicking solo on a channel creates a big banner element that misaligns everything.
