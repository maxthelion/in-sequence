# Resolution — branch fix/ui-consistency-bugs

- Trigger Type picker removed (only one algorithm exists).
- New shared `StudioRotaryKnob` control (Sources/UI/Theme/StudioRotaryKnob.swift).
- Pulses is the prominent primary knob; Steps / Offset / Pitch hidden behind
  a "…" disclosure.
- Velocity / Gate are now compact knobs instead of full-width sliders.
- Redundant explainer lines removed from the generator editor container.
