sends are falling out of the box

Screenshots:
- 04-mixer.png

RESOLVED 2026-06-20: Held each SENDS knob to a tight per-slot footprint (two slots + gap = the strip's 96pt content width) and shrank the send knob to size 26, clipping each slot, so the A/B pair now sits fully inside the channel strip box instead of spilling over its rounded edge. Verified in 04-mixer.png: both knobs sit inside the strip outline.
