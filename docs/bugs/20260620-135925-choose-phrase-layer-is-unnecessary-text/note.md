Choose phrase layer is unnecessary text. This is presumably linked to clicking the pattern/layer button, but there's a nav in between. This is kind of awkward.  Maybe keep this inside the top orange box, rather than making it a separate one to keep the connection. The other nav could be underneath, or temporarily hidden.

Screenshots:
- 11-phrase-layer-selector-open.png

RESOLVED 2026-06-20: Dropped the "CHOOSE PHRASE LAYER" title and moved the layer-selection grid inside the top orange shell (under a divider); the LAYERS/SCENES/GLOBAL APPLY tab row is hidden while selecting.
