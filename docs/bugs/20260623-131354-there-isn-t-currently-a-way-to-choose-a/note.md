There isn't currently a way to choose a slice for a given step. Ideally, this works in combination with the waveform above so that when a step is selected, you can click a portion of the waveform and it gets mapped

Screenshots:
- 23a-track-slicer-populated.png

## Decision (2026-06-23, user)
Build now: implement the slice-per-step feature (select a step, then click a region of the waveform to map that slice to the step). Show the interaction for review before finalizing.
