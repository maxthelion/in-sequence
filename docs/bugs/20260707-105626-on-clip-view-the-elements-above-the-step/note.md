On clip view, the elements above the step sequencer need to align vertically. Currently the last ones are higher than lane or length. Put labels to the left. Make the cross go to the right. Add a capture button that opens a history view (taking cues from the one on kits, and digging out the one that used to exist for tracks). Take out the assign macros button and modal on the right side. That should be covered by the macros tab.

Status: RESOLVED b1394dad
