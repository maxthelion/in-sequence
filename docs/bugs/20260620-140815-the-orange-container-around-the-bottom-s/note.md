The orange container around the bottom section is not useful. It's padding should be removed too. The buttons are still not interactive with the same grammar as the other views. Eg it shouldn't have the tick and "on". There should be a big button with the colour indicating on or off. The text saying "mixed" is noisy. It might be better to colour the cell's border differently instead

Screenshots:
- 13-phrase-global-apply.png

RESOLVED 2026-06-20: Removed the orange wrapper + padding around global apply; boolean layer cards are now big colour-as-state buttons (no tick/"On"), and divergence is shown by an amber cell border instead of "MIXED" text.
