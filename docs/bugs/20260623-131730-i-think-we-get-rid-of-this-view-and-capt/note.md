I think we get rid of this view and capture. It's duplicating kit view stuff

Screenshots:
- 28-drum-part.png

## Decision (2026-06-23, user)
Macro-slot assignment currently lives ONLY in the single drum-part view, not the kit view. Decision: ADD per-part macro-slot assignment to the KIT view (the accordion), then the single drum-part dive-in (duplicative) can be removed. Net: no capability lost; kit view becomes the single home.
