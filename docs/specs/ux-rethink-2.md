Have a button for current phrase. Make a phrase edit view. Show the phrase that is being edited above the matrix.

Layer perform. Selection perform. Different modes for changing the cells in the grid.

Phrase cell edit. Eg changing a value per bar. Selectable layer, selectable value? Cell mode (single value, bar etc.). Also pattern length, scene a b etc.

Phrase scene mode. Edits values for the scene.

Open question what does it mean to perform?

Track selection as a different mode of the phrase edit?

Tracks top level view - click on each to open. Make a selection, actions such as make a performance group.

Performance group limits a selection of tracks - for performing either layer or same value.

Is perform something that enables the capture mechanism for the phrase?

hierarchy

* Song (shows all phrases)
    * can jump to phrase view for any phrase
* Named current phrase (opens phrase view similar to current tracks in perform mode)
    * Phrase scenes (like modified current scene perform)
    * Phrase layers (perform for tracks)
    * Performance groups
        * Performance group perform switch between single layer, or same value for whole selection
    * Edit phrase cell (perhaps perform for a single track too)
* Tracks (opens simple track view)
    * Selection mode on/off. Actions include creating performance group
    * clicking opens track
    * Add new tracks
* Mixer (global default values - perhaps overridden in phrases later)
* Scenes (management of scenes)

This doesn't really cover the capture feature. But maybe that can wait
