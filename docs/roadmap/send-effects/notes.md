# Send Effects Notes

## Raw Intent

```text
re 6, let's just have 2 default busses for send a and b. Other tracks can route part of their output to them. Effects can be added as inserts.
```

## Clarified Concern

Captured from user clarification on 2026-04-29.

## Notes

re 6, let's just have 2 default busses for send a and b. Other tracks can route part of their output to them. Effects can be added as inserts.

## Cross-References

- Item `4`, Mixer Main Out: send busses sit inside the overall mixer structure alongside tracks, busses, and master out.
- Item `5`, Mixer Busses: send A and send B are default busses, distinct from user-added output busses.
- Item `5`, Mixer Busses: user-created busses should allow insert effects too, not only send A/B.
