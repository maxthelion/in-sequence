# Phrase Features Artifacts

## 2026-04-29 - Phrase Matrix Screenshot

The user attached a screenshot of the current Phrase Matrix showing:

- `Phrase A` as a left-side phrase button with `8 bars`.
- Track names across the top with matrix cells below.
- Track-page arrows and `Tracks 1 / 1` floating at the upper right.
- Layer selector/control area at the upper left, with variable width depending on the selected layer.

The screenshot highlights two layout concerns:

- Track page navigation should live as matrix cells in the top left and top right corners, either side of the track-name row, with disabled/available state indicating whether another track page exists.
- The layer selector should be more central, aligned with the grid, and have stable horizontal width across layers.
