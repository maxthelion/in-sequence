# Scenes In Phrases Artifacts

## 2026-04-29 - Phrase Matrix Screenshot

The user attached a Phrase Matrix screenshot showing the current `Tracks`-style phrase matrix:

- phrases are shown down the left;
- tracks are shown across the top;
- each phrase/track cell selects a pattern slot/layer value.

Design implication:

- the same phrase matrix region should be able to switch into a `Scenes` mode;
- in Scenes mode, the matrix should collapse to three columns: A, crossfader, B;
- each phrase row should configure scene A, crossfader behavior, and scene B.
