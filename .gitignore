# macOS
.DS_Store

# Xcode
*.xcodeproj
*.xcworkspace
DerivedData/
build/
*.xcuserstate
*.xcuserdatad/
xcuserdata/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/
Packages/
Package.resolved

# CocoaPods (not used but safe)
Pods/

# Generated / intermediate
*.tmp
.meta/
!.meta/
!.meta/multipass/
!.meta/multipass/README.md
!.meta/multipass/multipass.yaml
!.meta/multipass/config/
!.meta/multipass/config/**
!.meta/multipass/state/
!.meta/multipass/state/**
.meta/multipass/runtime/
.meta/project/
.meta/project-tick/
.routing-source-mixer-split-visual-evidence-output

# Editor
.vscode/
.idea/
.antigravitycli/

# Local config
*.local

# Claude Code runtime state (per-environment only; project-scoped config is committed)
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/logs/
.claude/cache/
# Behaviour-tree generated files — churn-on-every-run. Durable state
# (review-queue/, inbox/, work-item.md, candidates.md, last-*-sha) is committed.
.claude/state/next-action.md
.claude/state/perf-raw/

# Octowiki runtime state (regenerable caches / queues)
wiki/.meta/

# Git worktrees (isolated workspaces for feature branches)
.worktrees/
.claude/worktrees/

# Local AU hosting experiments
Experiments/PigmentsProbe/

build-dd/

.foreman/state/
