# Track Setup Surface Compression PM Loop

## 2026-07-06T16:47Z Decision Addendum

- decision:
  `.meta/multipass/runtime/loops/pm/track-setup-surface-compression/decide/2026-07-06T16-47Z-pm-decision.md`
- request:
  `.meta/multipass/runtime/inbox/claimed/2026-07-06T163139273Z-pm-decider-cadence.md`

Decision: no further PM artifact-author request is useful right now. The
builder-facing package under `docs/roadmap/track-setup-surface-compression/`
is PM-ready for future build promotion by the appropriate project/build
decider, but this PM decider did not promote a build loop or route
implementation.

Readiness boundary: future implementation should remain one compact
setup-surface visual-economy pass for G7 plus the fresh
`18-track-detail-steps-clip` clip-header report. It should require exact-state
visual evidence for row `18-track-detail-steps-clip` and touched
slicer/sample-player rows, UX canon lint for UI changes, and a builder final
mapping linked reports to fixed / unchanged-out-of-scope / split.

Product-owner lock: not needed unless the lane is broadened into AU runtime
safety, transient-detection algorithm quality, mixer/send follow-up, Scenes IA,
Track/Phrase Perform semantics, or broad drum-kit matrix architecture.

## 2026-07-06T16:52Z Orientation Addendum

- orientation:
  `.meta/multipass/runtime/loops/pm/track-setup-surface-compression/orient/2026-07-06T16-52Z-pm-orientation.md`
- request:
  `.meta/multipass/runtime/inbox/claimed/2026-07-06T163139137Z-pm-orienter-cadence.md`

Interpretation: the lane is a bounded setup-surface visual-economy package for
G7 plus the fresh `18-track-detail-steps-clip` clip-header report. It should
compress track/setup headers, move clip-local lane/length/layer/randomize/config
controls into or adjacent to the clip header, place slicer/sample-player controls
near the waveform or slice object, and remove repeated labels, grey helper copy,
and nested visual noise.

Lowest unmet PM layer: no longer initial artifact authoring. The builder-facing
package under `docs/roadmap/track-setup-surface-compression/` is coherent. The
remaining PM/readiness layer is decision/promotion discipline plus exact-state
evidence requirements. A decider can mark this package ready for future build
promotion when capacity permits, or request a narrow bug-to-acceptance mapping
refinement. Additional broad PM authoring is not indicated by current evidence.

Product-owner lock: not needed. The owner intent is concrete and repeated. A
lock is only needed if this lane is broadened into transient algorithm quality,
AU runtime safety, mixer/send follow-up, Scenes IA, Track/Phrase Perform
semantics, or broad drum-kit matrix implementation.

Build promotion read: PM package is plausible for a future bounded build, but
this orientation did not schedule work. Future promotion should require updated
row `18-track-detail-steps-clip`, touched slicer/sample-player visual evidence,
UX canon lint for UI changes, and a builder final mapping linked reports to
fixed / out-of-scope / unchanged / split.

- updated: 2026-07-06T16:43Z
- loop: `pm/track-setup-surface-compression`
- status: builder-facing-pm-package-drafted
- feature: `track-setup-surface-compression`
- registry manifest:
  `.meta/multipass/config/loops/pm/track-setup-surface-compression.yaml`
- runtime root:
  `.meta/multipass/runtime/loops/pm/track-setup-surface-compression/`
- product-doc root:
  `docs/roadmap/track-setup-surface-compression/`
- setup evidence:
  `.meta/multipass/runtime/loops/project/act/2026-07-06T16-22Z-track-setup-surface-compression-pm-lane.md`
- initial artifact-author request:
  `.meta/multipass/runtime/inbox/pending/2026-07-06T162149447Z-pm-artifact-author-track-setup-surface-compression.md`

## Compact PM Intent

This lane isolates bug-intake `G7: Slicer / Sample Player / Track Header
Compression` plus the fresh capture-backed clip-header visual-economy report
`docs/bugs/20260706-113305-move-lane-length-layer-chooser-randomize`.

The desired PM output is one bounded builder-facing slice around compact setup
headers, clip header control placement for lane/length/layer/randomize/config,
and slicer/sample-player visual economy. It should preserve the track setup
workflow while reducing vertical waste, duplicated titles, grey explainer
lines, and misplaced waveform/clip controls.

## Boundaries

Keep this lane separate from:

- `build/au-runtime-safety`
- mixer follow-up / send-channel polish
- Scenes IA / Scene Perform management
- Track/Phrase Perform interaction and selection
- broad drum-kit matrix implementation

The `docs/bugs/20260623-131606-*` transient-finding quality report is linked
through G7 but should be deferred or explicitly split if PM analysis finds it
is algorithm work rather than setup-surface visual economy.

## Artifact Result

Initial builder-facing PM package is now drafted under
`docs/roadmap/track-setup-surface-compression/`:

- `README.md` records the lane status, scope boundary, and evidence base.
- `builder-slice.md` defines one bounded buildable slice for compact setup
  headers, clip-header control placement, slicer/sample-player visual economy,
  waveform-adjacent controls, and removal of redundant grey/explainer text.

The package preserves the requested boundary from AU runtime safety, mixer
follow-up, Scenes IA, Track/Phrase Perform interaction, and broad drum-kit
matrix implementation. It explicitly splits
`docs/bugs/20260623-131606-i-feel-like-the-transient-finding-and-se` if the
remaining problem is transient-analysis quality rather than setup-surface
visual economy.

Lane readiness: PM handoff is ready for a future decider to promote when
ordinary build capacity and exact-state evidence needs permit. This actor did
not create/promote a build loop or schedule implementation.

## Current Interpretation

Feature-readiness currently reports no unpromoted builder-ready PM candidate,
and `build-capacity.ts` now reports one available ordinary build slot with
ready/unpromoted candidates `none`. This lane is therefore PM supply recovery:
it creates artifact-prep capacity for a coherent high-priority owner-feedback
cluster, but it does not assert builder readiness and does not consume the
ordinary build slot.

The first useful action is PM artifact authoring under
`docs/roadmap/track-setup-surface-compression/`, using the G7 bug list, the
fresh `18-track-detail-steps-clip` capture-backed bug, and current boundaries
as authority. Product-owner attention is not needed for lane setup.

That action has been completed for the initial PM package. Remaining readiness
work is build-loop promotion by a decider, implementation, and paired capture /
lint evidence.

## Checks / Evidence

- Read `.meta/multipass/state/bug-intake.md` G7.
- Read `.meta/multipass/state/holistic-status.md` 2026-07-06T14:26Z addendum.
- Read `.meta/multipass/state/feature-readiness.md`.
- Ran Foreman Coordinator `build-capacity.ts`; it reported active ordinary
  build loop `build/au-runtime-safety`, one available ordinary slot, and
  ready/unpromoted candidates `none` with known Ruby warning noise.
- No product code, build worktree, build-loop promotion, merge, rebase,
  visual automation, or request lifecycle move was performed.
