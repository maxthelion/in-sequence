id: build/mixer-strip-followup
kind: build
status: complete
feature: mixer-strip-followup
worktree: .worktrees/mixer-strip-followup
branch: feature/mixer-strip-followup
actors:
  orient:
    - actor: build-orienter
  decide:
    - actor: build-decider
  act:
    - actor: builder
      cadence: on_inbox
  observe:
    - actor: architecture-review
      cadence: on_inbox
    - actor: testing-review
      cadence: on_inbox
gates:
  - id: architecture
    actor: architecture-review
    checklist: architecture
  - id: testing
    actor: testing-review
    checklist: testing
