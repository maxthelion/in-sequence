id: build/drum-kit-matrix-sound-implementation
kind: build
status: complete
feature: drum-kit-matrix-sound-implementation
worktree: .worktrees/drum-kit-matrix-sound-implementation
branch: feature/drum-kit-matrix-sound-implementation
actors:
  orient:
    - actor: build-orienter
  decide:
    - actor: build-decider
  act:
    - actor: builder
      cadence: on_inbox
  observe:
    - actor: architecture-review
      cadence: on_inbox
    - actor: testing-review
      cadence: on_inbox
gates:
  - id: architecture
    actor: architecture-review
    checklist: architecture
  - id: testing
    actor: testing-review
    checklist: testing
